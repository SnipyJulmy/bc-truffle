class ParserTest extends BcTestSpec {
  behavior of "bc parser"

  it should "correctly parse string literal" in {}
  it should "correctly parse double literal" in {}
  it should "correctly parse term expression" in {}
  it should "correctly parse factor expression" in {}
  it should "correctly parse power expression" in {}
  it should "correctly parse neg (unary) expression" in {}
  it should "correctly parse post/pre inc/dec expression" in {}
  it should "correctly parse parenthesis expression" in {}
}
